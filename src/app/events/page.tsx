import { PhaseShellPage } from "@/components/phase-shell-page";

export default function EventsPage() {
  return (
    <PhaseShellPage
      title="Events"
      description="Events calendar route scaffolded and ready for dynamic data integration."
    />
  );
}
