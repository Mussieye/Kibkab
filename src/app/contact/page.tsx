import { PhaseShellPage } from "@/components/phase-shell-page";

export default function ContactPage() {
  return (
    <PhaseShellPage
      title="Contact"
      description="Contact route scaffolded and ready for office info, map, and contact channels."
    />
  );
}
